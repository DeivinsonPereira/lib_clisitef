import 'package:flutter/material.dart';

class ClisitefView extends StatelessWidget {
  const ClisitefView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}